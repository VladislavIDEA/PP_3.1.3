spring:
  application:
    name: projectPP_3.1.1

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/exampledb123?useSSL=false&serverTimezone=UTC
    username: root123
    password: "root123@vlad"

  jpa:
    properties:
      hibernate:
        show_sql: true
        hbm2ddl:
          auto: create-drop
        dialect: org.hibernate.dialect.MySQL8Dialect
  logging:
    level:
      org.springframework.security: DEBUG

  mvc:
    hiddenmethod:
      filter:
        enabled: true

